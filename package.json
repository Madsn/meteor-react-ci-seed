{
  "name": "meteor-react-playground",
  "private": true,
  "scripts": {
    "start": "meteor run"
  },
  "dependencies": {
    "classnames": "^2.2.5",
    "meteor-node-stubs": "~0.2.0",
    "react": "^15.0.2",
    "react-addons-pure-render-mixin": "^15.0.2",
    "react-dom": "^15.0.2"
  },
  "scripts": {
    "test": "meteor test --once --driver-package dispatch:mocha-phantomjs",
    "test-full-app": "meteor test --full-app --once --driver-package dispatch:mocha-phantomjs"
  }
}
